version: 1.0
meta:
  environment: docker
  toolchain:
    name: docker
    version: latest
build:
  dockerfile: Dockerfile
  skip: false
run:
  image: electrician-app
  command: java -jar app.jar
  args: ""
  persistenceMount: /data
  containerPort: 8081
serviceType: compute
